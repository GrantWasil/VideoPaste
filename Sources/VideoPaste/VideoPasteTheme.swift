import SwiftUI

enum VideoPasteTheme {
  /// A darker brand orange that maintains strong contrast with white action labels.
  static let primaryActionTint = Color(
    red: 154.0 / 255.0,
    green: 52.0 / 255.0,
    blue: 18.0 / 255.0
  )
}
